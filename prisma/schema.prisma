generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // The 'url' line MUST be removed from here in Prisma 7
}

model TestProfile {
  id   String @id @default(uuid())
  name String
}
